node_modules
dist
coverage
src/generated
.env
.env.*
!.env.example
# Uploads: o diretório é versionado (uploads/.gitkeep explica por quê), o
# conteúdo não.
uploads/*
!uploads/.gitkeep

# Originais das imagens do seed fake (9.11/AB1) — só o base64 derivado é versionado.
assets-inbox/

# Rascunho efêmero (9.12/AC5): nada daqui é versionado, exceto o README que
# explica o diretório para quem clona.
.scratch/*
!.scratch/README.md
