meta {
  name: Change Password
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/auth/change-password
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "currentPassword": "DemoOasis2026!",
    "newPassword": "NovaSenha2026!"
  }
}

docs {
  Requer Bearer. ⚠️ Muda a senha da conta logada e invalida todas as sessões — não rode com o usuário demo a menos que queira alterar a senha pública dele.
}
